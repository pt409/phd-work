<eam id="elemental EAM potential" species-a="*" species-b="*">

	<mapping>
		<pair-interaction species-a="*" species-b="*" function="V" />
		<electron-density species-a="*" species-b="*" function="rho" />
		<embedding-energy species="*" function="F" />			
	</mapping>

	<functions>
		<user-function id="V">
			<input-var>r</input-var>
			<expression>
			  A * exp(-alpha * (r / re - 1)) / (1 + ((r / re) - kappa)^20) 
			  - B * exp(-beta * (r / re - 1)) / (1 + ((r / re) - lambda)^20) 
			</expression>
			<derivative>
			  - A * alpha * exp(-alpha * (r / re - 1)) / (re * (1 + ((r / re) - kappa)^20))
			  - 20 * A * (r / re - kappa)^19 * exp(-alpha * (r / re - 1)) / (re * (1 + ((r / re) - kappa)^20)^2)
			  + B * beta * exp(-beta * (r / re - 1)) / (re * (1 + ((r / re) - lambda)^20))
			  + 20 * B * (r / re - lambda)^19 * exp(-beta * (r / re - 1)) / (re * (1 + ((r / re) - lambda)^20)^2)
			</derivative>
			<param name="re">2.74</param>
			<param name="A">0.421378</param>
			<param name="B">0.640107</param>
			<param name="alpha">8.679625</param>
			<param name="beta">4.629134</param>
			<param name="lambda">1.0</param>
			<param name="kappa">0.5</param>
		</user-function>
	      
		<user-function id="rho">
	      		<input-var>r</input-var>
	      		<expression>
			  fe * exp(-beta * (r / re - 1)) / (1 + ((r / re) - lambda)^20) 
	      		</expression>
	      		<derivative>
			  + fe * beta * exp(-beta * (r / re - 1)) / (re * (1 + ((r / re) - lambda)^20))
			  + 20 * fe * (r / re - lambda)^19 * exp(-beta * (r / re - 1)) / (re * (1 + ((r / re) - lambda)^20)^2)
	      		</derivative>
	      		<param name="fe">1.975299</param>
		</user-function>

	    	<user-function id="F">
	      		<input-var>rho</input-var>
	      		<expression>
			  (rho &lt; 0.85 * rhoe) ?
			  Fn0
			  + Fn1 * (rho / (0.85 * rhoe) - 1)
			  + Fn2 * (rho / (0.85 * rhoe) - 1)^2
			  + Fn3 * (rho / (0.85 * rhoe) - 1)^3
			  : (rho &lt; 1.15 * rhoe) ?
			  F0
			  + F1 * (rho / rhoe - 1)
			  + F2 * (rho / rhoe - 1)^2
			  + F3 * (rho / rhoe - 1)^3
			  :
			  Fe * (1 - eta * log(rho / rhos)) * ((rho / rhos)^eta)
	      		</expression>
		     	<derivative>
			  (rho &lt; 0.85 * rhoe) ?
			  Fn1 / (0.85 * rhoe)
			  + Fn2 * 2 * (rho / (0.85 * rhoe) - 1) / (0.85 * rhoe)
			  + Fn3 * 3 * (rho / (0.85 * rhoe) - 1)^2 / (0.85 * rhoe)
			  : (rho &lt; 1.15 * rhoe) ?
			  F1 / rhoe
			  + F2 * 2 * (rho / rhoe - 1) / rhoe
			  + F3 * 3 * (rho / rhoe - 1)^2 / rhoe
			  :
			  - Fe * eta * ((rho / rhos)^eta) / rho
			  + Fe * eta * ((rho / rhos)^(eta - 1)) * (1 - eta * log(rho / rhos)) / rhos 
		      	</derivative>
			<param name="Fn0">-2.541799</param>
			<param name="Fn1">-0.219415</param>
			<param name="Fn2">0.733381</param>
			<param name="Fn3">-1.589003</param>
			<param name="F0">-2.56</param>
			<param name="F1">0.0</param>
			<param name="F2">0.705845</param>
			<param name="F3">-0.687140</param>
			<param name="Fe">-2.559307</param>
			<param name="rhoe">27.206789</param>
			<param name="rhos">27.206789</param>
			<param name="eta">0.694608</param>
		</user-function>

	</functions>

</eam>
